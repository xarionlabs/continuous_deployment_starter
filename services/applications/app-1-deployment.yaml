apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose --file ./docker-compose.yml convert
    kompose.version: 1.34.0 (HEAD)
  labels:
    io.kompose.service: app-1
  name: app-1
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: app-1
  template:
    metadata:
      annotations:
        kompose.cmd: kompose --file ./docker-compose.yml convert
        kompose.version: 1.34.0 (HEAD)
      labels:
        io.kompose.service: app-1
    spec:
      containers:
        - image: ghcr.io/xarionlabs/continuous_deployment_starter/app_1:20250128-135725
          name: app-1
          ports:
            - containerPort: 8501
              protocol: TCP
      restartPolicy: Always
