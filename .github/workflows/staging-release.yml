name: staging-release
on:
  workflow_run:
    workflows: ["build"]
    branches: [main]
    types: 
      - completed

jobs:
  release:
    runs-on: ubuntu-latest
    environment: staging
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    steps:
      - name: executing remote ssh commands using ssh key
        uses: appleboy/ssh-action@v1.2.0
        env:
          VARS_JSON: ${{ toJson(vars) }}
        with:
          host: ${{ vars.HOST }}
          username: ${{ vars.USERNAME }}
          key: ${{ secrets.KEY }}
          envs: VARS_JSON
          permissions: |
            secrets: read
          script: |
            set -e
            cd ~/runtime/continuous_deployment_starter
            export PATH=$PATH:$(pwd)/.github/workflows/scripts/
            git checkout releases
            git pull origin releases
            refresh_podman_secrets.sh "${{ github.repository }}" "${{ secrets.GITHUB_TOKEN }}" staging
            create_and_source_env_variables.sh
            check-service-envs.sh
            generate_quadlets.sh
            start_all_services.sh